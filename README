A simple tool which can auto-update `updated` field of front-matter in the hexo posts.

Prerequisites:
    Linux 2.6.13 or above
    gcc and glibc
    make

Compile:
    just run: make

Usage:
    just run: ./tmup /path/to/posts